# Formatted Print

from pprint import pprint
my_dict = {"a": list(range(1, 11)), "b": list(range(11, 21)), "c": list(range(21, 31))}
pprint(my_dict)

# pprint -  pretty printer, print in well-formated and readable way